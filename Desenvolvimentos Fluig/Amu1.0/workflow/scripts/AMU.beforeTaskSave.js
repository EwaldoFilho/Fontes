function beforeTaskSave(colleagueId, nextSequenceId, userList) {

    // if (nextSequenceId == 5 || nextSequenceId == "5") {
    //     var ParentFolder = "3177";
    //     //var NumProcess = hAPI.getCardValue("AMUnumero");
    //     var NumProcess = getValue("WKNumProces");

    //     var signatureImageData1 = hAPI.getCardValue("signatureImageData1");

    //     if (signatureImageData1 != "" && signatureImageData1 != null) {

    //         var cst1 = DatasetFactory.createConstraint("PARENTDOCUMENTID", ParentFolder, ParentFolder, ConstraintType.MUST);
    //         var cst2 = DatasetFactory.createConstraint("FOLDERDESCRIPTION", NumProcess, NumProcess, ConstraintType.MUST);
    //         var cst3 = DatasetFactory.createConstraint("DOCUMENTDESCRIPTION", "Assinatura Medico SME", "Assinatura Medico SME", ConstraintType.MUST);
    //         var cst4 = DatasetFactory.createConstraint("FILENAME", "assinaturaMedicoSME.png", "assinaturaMedicoSME.png", ConstraintType.MUST);
    //         var cst5 = DatasetFactory.createConstraint("FILECONTENT", signatureImageData1, signatureImageData1, ConstraintType.MUST);

    //         var filtro = new Array(cst1, cst2, cst3, cst4, cst5);

    //         DatasetFactory.getDataset("dsCreateDocumentAndFolderCredenciamento", null, filtro, null);

    //     }
    // }
}